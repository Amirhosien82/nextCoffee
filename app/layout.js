export const metadata = {
  title: {
    template: "نِکست کافه | %s",
    default: "نِکست کافه",
  },
};

function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>
        <header></header>
        <main></main>
        <footer></footer>
      </body>
    </html>
  );
}

export default RootLayout;
