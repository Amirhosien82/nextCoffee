import styled from "./Loading.module.css";

function Loading() {
  return (
    <div className={styled.container}>
      <span class={styled.loader}></span>
    </div>
  );
}

export default Loading;
